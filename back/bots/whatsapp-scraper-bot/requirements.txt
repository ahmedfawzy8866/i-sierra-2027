# ── Sierra Estates "back" — Python Autopilot Runtime ──────────────────────────
# Pinned to resolve the firebase-admin ⨯ google-generativeai dependency triangle.
#
# THE CONFLICT (and why these pins work):
#   firebase-admin ──> google-cloud-firestore, google-cloud-storage
#                 └──> google-api-core, proto-plus, protobuf
#   google-generativeai ──> google-ai-generativelanguage ──> google-api-core, protobuf
#
#   Both branches converge on `google-api-core` and `protobuf`. Conflicts happen when
#   one pulls protobuf 6.x while the other caps at <5. The versions below all sit on
#   the SAME google-api-core (2.x) + protobuf (>=4.25,<6) floor, so pip resolves clean.
#
# Verify after install:  pip check        (must print "No broken requirements found.")

firebase-admin==6.5.0            # brings google-cloud-firestore 2.x + google-cloud-storage 2.x
google-generativeai==0.8.3       # Gemini SDK; shares google-api-core 2.x / protobuf 4-5
google-cloud-firestore==2.19.0   # pinned explicitly so firebase-admin can't float it
protobuf==4.25.5                 # single source of truth for the proto runtime (both branches accept)
python-dotenv==1.0.1             # .env credential vault loader
tenacity==9.0.0                  # retry/backoff for Gemini + Firestore calls
requests==2.32.3                 # Telegram heartbeat / health pings
